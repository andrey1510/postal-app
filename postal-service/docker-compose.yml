services:

  db:
    container_name: updates-database
    image: postgres:15
    environment:
      - POSTGRES_DB=updatesbase
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=password
    ports:
      - 5556:5432
    restart: unless-stopped