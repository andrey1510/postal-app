spring:
  application:
    name: packing-list--service
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8889

management:
  endpoints:
    web:
      exposure:
        include: *