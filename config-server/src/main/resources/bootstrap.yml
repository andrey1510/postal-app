spring:
  application:
    name: config-server
  cloud:
    config:
      server:
#        bootstrap: true
        native:
          search-locations: classpath:/config

server:
  port: 8889

management:
  endpoints:
    web:
      exposure:
        include: *